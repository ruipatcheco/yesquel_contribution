#extend the original alpine image
FROM ubuntu:16.04
#copy the starting script to the container image
#COPY entrypoint.sh /entrypoint.sh
#run the custom script using the “sh” shell
#CMD ["sh", "/entrypoint.sh"]
